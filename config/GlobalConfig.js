const config = {
    APIWaitTime: 20,        // Time in ms to wait between requests to an API
    RefreshDatabase: true,  // Determines if RuneScape API should be called to periodically refresh data tables
    Logging_Debug: true     // Enables general Debug logging statements through utils/logger.js
};

module.exports = config;